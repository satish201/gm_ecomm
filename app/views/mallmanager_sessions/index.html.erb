<h1>Listing mallmanager_sessions</h1>

<table>
  <tr>
    <th>Username</th>
    <th>Password</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @mallmanager_sessions.each do |user_session| %>
  <tr>
    <td><%= mallmanager_session.username %></td>
    <td><%= mallmanager_session.password %></td>
    <td><%= link_to 'Show', mallmanager_session %></td>
    <td><%= link_to 'Edit', edit_mallmanager_session_path(user_session) %></td>
    <td><%= link_to 'Destroy', mallmanager_session, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New User session', new_mallmanager_session_path %>
