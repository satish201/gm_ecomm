<h1>Listing mapviews</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @mapviews.each do |mapview| %>
  <tr>
    <td><%= link_to 'Show', mapview %></td>
    <td><%= link_to 'Edit', edit_mapview_path(mapview) %></td>
    <td><%= link_to 'Destroy', mapview, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Mapview', new_mapview_path %>
